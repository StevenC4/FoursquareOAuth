var React = require('react/addons');

var Header = React.createClass({
      render: function () {
        return (
          <div id="header">
	    Header bar
          </div>
        )
      }
  });

module.exports = Header;
